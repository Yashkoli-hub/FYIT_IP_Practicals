import numpy as np
data = np.mean([1, 2, 3, 4])
# Mean
mean_val = np.mean(data)
print("Mean:", mean_val)

# Median
median_val = np.median(data)
print("Median:", median_val)

# Standard Deviation
std_val = np.std(data)
print("Standard Deviation:", std_val)
